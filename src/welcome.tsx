import React from 'react';
import {View,Text,StyleSheet} from 'react-native';

const welcome = () => {
    return(
        <View  >
            <Text style={styles.red} >welcome all guys!</Text>
        </View>
    );
};

const styles=StyleSheet.create({
    red:{git 
        color:'red'
 }
});
export default welcome;